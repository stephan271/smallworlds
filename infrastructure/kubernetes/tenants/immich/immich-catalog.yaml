apiVersion: postgresql.cnpg.io/v1
kind: ClusterImageCatalog
metadata:
  name: immich-catalog
spec:
  images:
    # Immich v3 requires the VectorChord extension (pgvecto.rs is deprecated)
    - image: "ghcr.io/tensorchord/cloudnative-vectorchord:16.13-1.1.1"
      major: 16
