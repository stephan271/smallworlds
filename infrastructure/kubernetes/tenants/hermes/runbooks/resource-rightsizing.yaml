name: Pod Resource Right-Sizing
description: Analyzes 7-day P95 usage and recommends requests/limits adjustments
schedule: "0 0 * * 0" # Weekly on Sunday
tasks:
  - id: analyze-usage
    action: script
    script: |
      # Query Prometheus for 7-day P95 CPU/Memory per pod
      # Compare against manifest limits
      # If delta > 50%, generate PR with recommendations
      echo "Analyzing P95 resource usage..."
