name: VM Vertical Scaling
description: Proposes Hetzner Server upgrades/downgrades based on sustained usage
triggers:
  - alertmanager: SustainedNodeCPUHigh
  - alertmanager: SustainedNodeMemoryHigh
tasks:
  - id: propose-scaling
    action: script
    script: |
      # Check current server type in terraform state/api
      # Determine next server type in ladder
      # Check cost impact vs budget constraint
      # Generate PR modifying main.tf server_type
      # Require human approval and maintenance window
      echo "Proposing VM scale up..."
