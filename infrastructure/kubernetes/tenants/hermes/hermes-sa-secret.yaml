apiVersion: v1
kind: Secret
metadata:
  name: hermes-github-secret
  namespace: hermes
type: Opaque
stringData:
  # This should be replaced with a real token in production, usually injected by the setup script
  token: "REPLACE_WITH_GITHUB_PAT"
