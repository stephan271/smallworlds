apiVersion: apps/v1
kind: Deployment
metadata:
  name: bulwark
  namespace: bulwark
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bulwark
  template:
    metadata:
      labels:
        app: bulwark
    spec:
      containers:
        - name: bulwark
          # v1.4.8 shipped a broken build (v1.4.7 UI with mismatched Next.js
          # server actions — the login page's auto-SSO action 404s and users
          # are stuck on "Sign in with SSO")
          image: ghcr.io/bulwarkmail/webmail:v1.5.0@sha256:4a784a5d9574d2996ac42e395c685c90a66a4691eefc5ab77bb61fe78e397e51
          ports:
            - containerPort: 3000
          env:
            - name: JMAP_SERVER_URL
              value: "https://mail.smallworlds.network"
            - name: OAUTH_ENABLED
              value: "true"
            - name: OAUTH_ONLY
              value: "true"
            - name: OAUTH_ISSUER_URL
              value: "https://identity.smallworlds.network/realms/smallworlds"
            - name: OAUTH_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: keycloak-secret
                  key: clientId
            - name: OAUTH_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: keycloak-secret
                  key: client-secret
            - name: SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: session-secret
                  key: session-secret
            - name: APP_NAME
              value: "SmallWorlds Mail"
            - name: STALWART_FEATURES
              value: "true"
          volumeMounts:
            - name: bulwark-data
              mountPath: /app/data/admin
              subPath: admin
            - name: bulwark-data
              mountPath: /app/data/admin-state
              subPath: admin-state
      volumes:
        - name: bulwark-data
          persistentVolumeClaim:
            claimName: bulwark-data
---
apiVersion: v1
kind: Service
metadata:
  name: bulwark
  namespace: bulwark
spec:
  selector:
    app: bulwark
  ports:
    - name: http
      port: 80
      targetPort: 3000
