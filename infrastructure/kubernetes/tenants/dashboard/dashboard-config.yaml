apiVersion: v1
kind: ConfigMap
metadata:
  name: dashboard-config
  namespace: dashboard
data:
  settings.yaml: |
    title: SmallWorlds
    theme: dark
    color: slate
    layout:
      Applications:
        style: row
        columns: 4
  kubernetes.yaml: |
    mode: cluster
  services.yaml: |
    - Applications: []
